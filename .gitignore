# Dependencies
node_modules/

# Build output — Vercel builds this itself, don't commit it
client/dist/

# Environment files — NEVER commit secrets
.env
.env.local
.env.production

# OS / editor
.DS_Store
Thumbs.db
.vscode/
